# AI Study Buddy — Production Requirements
# Lean set for Railway/Render deployment

# Core HTTP utilities
requests>=2.31.0

# PDF & document parsing
pypdf>=4.1.0
pdfplumber>=0.11.0
python-pptx>=0.6.23
Pillow>=10.2.0

# AI / LLM providers
google-generativeai>=0.4.1
openai>=1.14.0

# Vector search (CPU-only, no GPU needed)
faiss-cpu>=1.8.0
numpy>=1.26.4
sentence-transformers>=2.5.1

# OCR (optional — only active when pytesseract is available on the OS)
pytesseract>=0.3.10
opencv-python-headless>=4.9.0.80
