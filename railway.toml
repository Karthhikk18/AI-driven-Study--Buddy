# Railway deployment config for the Python backend

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python backend/app/server.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "study-buddy-backend"
